from django.contrib import admin

#from django.contrib.auth.models import User
# Register your models here

#from .models import Profile

#admin.site.site_header = "Our Network Admin"


# Register your models here.

#@admin.register(Profile)
#class Profile(admin.ModelAdmin):
    #list_display = ('username', 'first_name', 'last_name', 'email')

    # 'need_job', 'medical', 'single', 'volunteer')
    # list_display = [field.name for field in Register._meta.get_fields()]
    #ordering = ('username',)

    #search_fields = ('username', 'first_name', 'last_name', 'email')
    #list_filter = ('username', 'first_name', 'last_name', 'email')
# 'financial_help', 'email', 'single', 'need_job', 'medical',
#          'volunteer', 'need_training',
#               'know_job', 'driving', 'username',
